import setuptools
# packages = setuptools.find_packages()
# print(packages)

setuptools.setup(
    name="moco",
    version="0.0.1",
    description="Moco: Meta-Optimization for Combinatorial Optimization",
    url="",
    packages=setuptools.find_packages(),
    python_requires='>=3.8'
)